export type SlimEngine = Record<string, unknown>;

export async function loadSlim(_engine: SlimEngine): Promise<void> {
  // This stub intentionally does nothing. In production, the real library would
  // augment the engine instance with slim bundle features.
}
